services:
  - type: web
    name: ai-advisor-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    rootDir: Dashboard/AiAdvisorApi
    envVars:
      - key: OPENAI_API_KEY
        sync: false

