<?xml version="1.0" encoding="utf-8" ?>
<!-- Code written for Interactive Department Map Page -->
<ContentPage
    x:Class="Dashboard.Pages.DepartmentMapPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:Dashboard.Controls"
    Title="Department Map">

    <!-- Full Screen WebView with Zoom and Pan Support -->
    <Grid RowDefinitions="Auto,*">
        <!-- TopBar -->
        <controls:TopBar Grid.Row="0" />
        
        <!-- WebView Container with Zoom Slider -->
        <Grid Grid.Row="1"
              RowDefinitions="Auto,*">
            <!-- Back Button -->
            <Frame Grid.Row="0"
                   BackgroundColor="#003087"
                   Padding="10"
                   HasShadow="True"
                   Margin="0,0,0,10">
                <Button x:Name="BackButton"
                        Text="← Back to Maps"
                        BackgroundColor="#002a54"
                        TextColor="White"
                        FontSize="14"
                        FontAttributes="Bold"
                        CornerRadius="8"
                        Padding="15,8"
                        HorizontalOptions="Start"
                        Clicked="OnBackButtonClicked" />
            </Frame>
            
            <!-- WebView with Zoom Slider -->
            <Grid Grid.Row="1">
                <WebView x:Name="InteractiveMapWebView"
                         VerticalOptions="FillAndExpand"
                         HorizontalOptions="FillAndExpand"
                         BackgroundColor="White"
                         IsVisible="True"
                         Opacity="1.0"
                         Navigating="OnWebViewNavigating"
                         Navigated="OnWebViewNavigated" />
                
            </Grid>
        </Grid>
    </Grid>
</ContentPage>

